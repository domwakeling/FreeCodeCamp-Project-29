import React from 'react';

export default class MenuItems extends React.Component {
    // Render all menu items, is repeated twice in Navbar (for full and
    // shrunk menu options)
    render() {
        return (
            <ul />
        );
    }
}
